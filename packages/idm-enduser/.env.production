# Production environment - values are runtime placeholder tokens.
# variable_replacement.sh (via entrypoint.sh) uses envsubst at container start

VUE_APP_IDM_URL=$IDM_REST_URL
VUE_APP_ADMIN_URL=$IDM_ADMIN_URL
VUE_APP_ENABLE_SELF_SERVICE=$ENABLE_SELF_SERVICE
THEME=default
